apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "visualization-api.fullname" . }}-test-connection"
  labels:
    {{- include "visualization-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "visualization-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
